# IZStepProjectPython — IKEA Furniture: EDA, Hypotheses, Price Prediction

Навчальний проєкт з аналізу даних меблів IKEA. Реалізовано повний пайплайн: **очищення даних → EDA → перевірка гіпотез → навчання моделей регресії для прогнозування ціни**.

---

## ✅ Що зроблено в проєкті

1. **Очищення даних**

   - завантаження датасету
   - обробка пропусків/аномалій
   - збереження підготовленого датасету

2. **EDA (Exploratory Data Analysis)**

   - описова статистика
   - аналіз пропусків
   - візуалізації розподілів та зв’язків

3. **Перевірка гіпотез**

   - формулювання H0/H1
   - статистичні тести
   - висновки за p-value

4. **Моделювання (Price Prediction)**
   - baseline модель
   - feature engineering (додаткові ознаки)
   - порівняння кількох моделей
   - GridSearchCV для RandomForest
   - оцінка якості (R², MAE, RMSE)
   - feature importance (вбудована + permutation)
   - cross-validation (cv=5)

---

## 📊 Дані

- `ikea.csv` — сирі дані
- `ikea_clean.csv` — очищені дані (використовуються в EDA, гіпотезах та моделях)

Приклади ознак:

- `price` — цільова змінна
- `depth`, `width`, `height` — розміри
- `category`, `other_colors`, `designer_norm` — категоріальні ознаки

---

## 🧰 Технології

- Python 3.10+ (рекомендовано)
- numpy, pandas
- matplotlib
- scikit-learn

---

## ⚙️ Встановлення та запуск

### Клонування репозиторію

```bash
git clone <YOUR_REPO_URL>
cd IZStepProjectPython

```

## Віртуальне середовище (venv)

### macOS / Linux

```bash
python -m venv .venv
source .venv/bin/activate

```

### Windows (PowerShell)

```bash
python -m venv .venv
.venv\Scripts\Activate.ps1

```

## Встановлення залежностей

```bash
pip install numpy pandas matplotlib scikit-learn

```

## Запуск скриптів

```bash
python <NAME_SCRIPTS>
```

#### Ihor Zolotoverkh
